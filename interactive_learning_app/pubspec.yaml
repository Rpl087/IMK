   name: interactive_learning_app
   description: A learning app for children, including those with disabilities.
   version: 1.0.0+1

   environment:
     sdk: ">=2.12.0 <3.0.0"

   dependencies:
     flutter:
       sdk: flutter
     cupertino_icons: ^1.0.2
     flutter_tts: ^3.2.2  # For text-to-speech functionality
     provider: ^6.0.2  # State management

   dev_dependencies:
     flutter_test:
       sdk: flutter

   flutter:
     uses-material-design: true
